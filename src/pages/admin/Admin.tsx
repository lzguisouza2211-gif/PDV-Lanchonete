import React from 'react'

// Página Admin (POS) — deverá ser protegida por role 'admin' posteriormente
export default function Admin(): JSX.Element {
  return (
    <main>
      <h1>Admin — stub (migrar lógica de Admin.js)</h1>
      <p>Rota prevista para ser protegida por role="admin".</p>
    </main>
  )
}
